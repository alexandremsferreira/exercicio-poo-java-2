
package agenda;

public class Contato extends Pessoa {
    public Contato(String nome, String email, String dataAniversario) {
        this.setNome(nome);
        this.setEmail(email);
        this.setDataAniversario(dataAniversario);
    }
}
